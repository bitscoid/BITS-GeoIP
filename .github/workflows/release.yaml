name: Release
on:
  workflow_dispatch:
    inputs:
      tag:
        description: "The tag version you want to build"
  schedule:
    - cron: "0 8 12 * *"
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Run lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest

      - name: Build geoip
        id: build
        run: go run -v .
        env:
          FIXED_RELEASE: ${{ github.event.inputs.tag }}

      - name: Generate sha256 hash
        if: steps.build.outputs.skip != 'true'
        run: |
          sha256sum geoip.db > geoip.db.sha256sum
          sha256sum geoip-id.db > geoip-id.db.sha256sum
          tar czf rule-set.tar.gz rule-set

      - name: Push rule-set branch
        if: steps.build.outputs.skip != 'true'
        uses: ./.github/actions/push-branch
        with:
          branch: rule-set
          files: |
            rule-set
            rule-set.tar.gz

      - name: Push release branch
        if: steps.build.outputs.skip != 'true'
        uses: ./.github/actions/push-branch
        with:
          branch: release
          files: |
            geoip.db
            geoip.db.sha256sum
            geoip-id.db
            geoip-id.db.sha256sum

      - name: Delete older releases
        if: steps.build.outputs.skip != 'true'
        uses: dev-drprasad/delete-older-releases@v0.3.5
        with:
          keep_latest: 3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Release geoip
        if: steps.build.outputs.skip != 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.build.outputs.tag }}
          files: |
            geoip.db
            geoip.db.sha256sum
            geoip-id.db
            geoip-id.db.sha256sum